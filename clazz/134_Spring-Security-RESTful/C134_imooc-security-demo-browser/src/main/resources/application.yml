server:
  port: 80
  session:
    timeout: 60 # session超时时间为60秒,这是最短的分钟数
spring:
  # Cannot determine embedded database driver class for database type NONE
  datasource:  
    # Establishing SSL connection without server's identity verification is not recommended. According to MySQL 5.5.45+, 5.6.26+ and 5.7.6+ requirements SSL connection must be established by default if explicit option isn't set. For compliance with existing applications not using SSL the verifyServerCertificate property is set to 'false'. You need either to explicitly disable SSL by setting useSSL=false, or set useSSL=true and provide truststore for server certificate verification.
    url: jdbc:mysql://yuefei.cf:13306/imooc_c134?useUnicode=true&characterEncoding=UTF-8&useSSL=false&characterSetResults=utf8&serverTimezone=GMT
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  # No Spring Session store is configured: set the 'spring.session.store-type' property
  session:
    store-type: none
# 禁用 身份认证
#security:
#  basic:
#    enabled: false

#yafey:
#  security:
#    browser:
#      loginPage: /demo-login.html

#yafey:
#  security:
#    browser:
#      loginType: REDIRECT
yafey:
  browser:
    signUpUrl: /demo-signUp.html
  security:
    code:
      image:
        length: 6
        width: 100
        url: /users #,/user/*
    social:
      filterProcessesUrl: /qqLogin
      qq:
        app-id: 
        app-secret: 
        providerId: callback.do
      weixin:
        app-id: wxd99431bbff8305a0
        app-secret: 60f78681d063590a469f1b297feff3c4
